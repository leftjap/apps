// 광고·핫딜·포인트글 판정 (제목 접두사, 소프트 플래그 — 하드 삭제 안 함).
// 2026-07-10 실측: 브랜드명은 대괄호 필수 ("쿠팡에서 산 물건 후기" 오탐 방지),
// 구 정규식은 "[네이버페이]…" 를 놓쳤음 (여는 대괄호 미고려). 완전성은 수집 데이터로 튜닝.
const AD_RE = /^(?:\[?AD\b|\[(?:쿠팡|쿠폰|네이버페이|네페)|핫딜|무료나눔|아직 엄카)/

export const isAd = (title) => AD_RE.test(title.trim())
