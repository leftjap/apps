# Supabase 프로젝트 설정 — taste (geo-apps 공유 프로젝트, 테이블 prefix taste_).
# 적용: supabase functions deploy generate-taste-reco --project-ref tcbooffrdacfatywdzcm

project_id = "tcbooffrdacfatywdzcm"

[functions.generate-taste-reco]
# 버튼은 유저 JWT, cron/수동은 x-taste-reco-token — 함수 본체에서 둘 다 처리하므로 gateway JWT 강제 안 함.
verify_jwt = false
