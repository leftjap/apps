# 발음 음차·채점 세션 핸드오프 (2026-08-31)

새 세션은 이 파일만 읽으면 이어서 작업할 수 있다. 이 세션의 실측 스크립트는 세션 전용
스크래치패드에 있어 소멸한다 — 재현 절차는 [lesson-explanation-guide-en.md](./lesson-explanation-guide-en.md)
§7 의 재현 블록과 PA 절에 남겼고, Azure 접근 경로는 auto memory `study-azure-speech-local-probe` 에 있다.

## 이 세션에서 확정된 사실 (전부 실측 근거)

1. **채점 버그 (사용자 지적)**: Azure PA 는 ReferenceText 의 사전 발음에 음소를 정렬해
   채점하므로, 축약(모음이 schwa 로 무너져 음소가 사라지는 발화)일수록 깎였다.
   "What do you mean" 또박또박 93.0 vs 축약(whaddaya) 86.7 (en-US 3보이스).
   what·mean 은 97 유지, do(→80)·you(→66) 만 하락 — 앱이 실제 발음을 가르쳐 놓고
   그대로 하면 벌을 주는 구조였다. → **수정 완료** (아래 커밋 2).

2. **guide-en §7 '연음 경계 재분절 의무'는 근거가 반증됐다**: §7 표의 정본 예시 6쌍을
   그 문서의 실측 절차(ko-KR TTS 음차 → 영어 STT)로 ko-KR 9화자 재현 →
   개선 0 · 후퇴 4 · 동률 2. 채택 근거(캐뉴 푸리론 5/5 vs 4/5, n=5)는 9화자에서 8/9 동률.
   core100 시드에 규칙을 전면 적용한 A/B 도 같은 방향 (정확일치 38/108 → 30/108).
   → 전면 재음차와 게이트 차단 승격을 철회하고 §7 에 "재검토 대기" 기록.

3. **재음절화 발음 자체는 채점 무감점**: that again→[ðæɾəɡɛn]·missed it→[ˈmɪstɪt] 등
   음소를 잃지 않는 연음은 PA 점수가 동률이거나 오른다. 깎이는 것은 강한 축약뿐.
   레퍼런스를 축약형으로 '교체'하면 또박또박 발화가 50.0 으로 무너진다(단어 점수 3).

## 완료된 커밋 (push 됨)

- `01a9d22` — 시드 소수정 2종(예스러r데이→예스터r데이 flap 정정, 언더r스투쥬 붙임 통일) +
  validate-seed 연음 경고 오탐 2종 제거(활음 시작 음절·청크 경계, 경고 42→27곳) +
  guide-en §7 실측 기록. 차단 승격은 철회.
- `4f5ff51` — `contractedReference()`(축약 13종) + `analyzeWavRest` 이중 채점(사전·축약
  레퍼런스 병행, 높은 쪽 채택). 효과: What do you mean 86.7→94.7 (짧은 문장에 집중,
  8단어 문장은 91.3→92.3). 부작용 0 실측(또박또박 무감점, 엉뚱한 발화 42.7 그대로).
- 검증: `pnpm test` 1415/1415 통과 (2026-08-31). 실 Azure 로 효과·부작용 실측 완료.

### 2026-09-01 후속 (정정 + 시간 단축)

- ⚠ **위 "부작용 0"은 AccuracyScore 층 한정이었다.** 실 Azure 재실측에서 PA 가 인식 Display 를
  레퍼런스 철자로 끌어당기는 것을 확인 — 축약 채택 시 `recognizedText` 가 `Whaddaya mean?` 이 되고,
  감점제 커버리지가 원문 대조에서 3단어 누락으로 오인해 **화면 점수가 13점으로 붕괴**했다
  (2026-08-31 감점제 화면 전환과 결합된 회귀). 수정: 전사는 사전 레퍼런스 응답에서 확정.
- 같은 커밋: `don't you`→`doncha` 가 라이브 경로에서 사문이던 결함 수정(아포스트로피 선택화),
  `contractedRef` 를 pronunciationLog 에 영속(채택 여부 직접 구별).
- 시간 단축(사용자 결정 "최대한 짧게"): 무음 대기 1.5→1.4초, 선채점 0.7→0.5초, **선채점 결과가
  전 단어 발화(omissions 0)로 확인되면 남은 대기를 버리고 즉시 종결**(en 한정, ja 는 omission
  판정 미실측이라 제외). 실측 RTT 0.47~0.77초(koreacentral·Wi-Fi) 기준 발화 끝→점수 ~1.0~1.3초
  (종전 ~1.6초). 부분 발화 실측 3종(2/4·3/4·4/7 단어)에서 Azure 가 미발화 단어를 전부 omission
  으로 표시했고 전사도 말한 부분까지만 나와, 문장 중간 쉼에는 조기 종결이 걸리지 않는다.
- 리뷰 지적 반영 2건: sync pull 이 `contractedRef` 를 지우던 것(LOCAL_ONLY 추가), 낡은 선채점
  발사의 늦은 실패가 새 발사를 무장 해제하던 것(promise 동일성 검사).
- 리뷰가 확인한 기존 한계 2건(이번 범위 밖, 미수정): (a) 전사가 빈 문자열이면 감점제가 0점으로
  붕괴하는 경로(judgeCoverage 가 전 단어 누락 판정 — 발생 빈도는 이론적), (b) recCtrl 대입 전
  onAutoStop 이 오면 재클릭 1회로 복구되는 UI 고착(기존 VAD 경로에도 동일).

### "과거 점수 0 표시" 재발 계열의 구조 봉합 (2026-09-01 저녁)

같은 증상(과거 발화 점수가 화면에서 0 으로 보임)이 진입 경로를 바꿔 세 번 재발했다:
재청취 fresh(8/31 수화 신설) → 신규 fresh(`ce261fd`) → 신규 restore(`74fc34c`). 원인은 수화를
분기마다 개별 배선하는 구조 자체였고, 감사에서 같은 계열의 잔여 구멍 2곳(재청취 restore,
복습 restore)을 실패 테스트로 추가 실증했다. 봉합: 분기별 배선을 폐기하고 **모든 진입 경로의
말미에서 무조건 이력 병합**(session-new `hydrateScores` 헬퍼 — 진입 exit 2곳, 복습은 분기 밖
1곳)으로 통일했다. 수화가 한 번 실패해도 다음 진입이 재시도하므로 빈 스냅샷 고착 루프가
구조적으로 끊긴다. 남은 재발 여지(구조 밖): 배포 직후 10분의 sw.js CDN 캐시 창(구버전 혼동,
GH Pages 한계), 드릴 텍스트가 바뀌면 이력 키 미매칭(재INSERT 금지 규약이 방어).
- 검증: `pnpm test` 1425/1425 (2026-09-01). base 1415 에서 다른 세션 잔존으로 working tree 의
  `sessionAnalyze.test.js`(26건)가 삭제돼 있던 것을 복원했고, 신규 테스트 10건을 더했다.

### 2026-09-06 — 숫자 표기 오누락 수정 + 억양 기준점 90 (scoreModel `ded2`)

- **발견 1 (실 Azure 실측)**: Display 는 숫자 단어를 숫자로 바꿔 쓴다("at seven"→"at 7:00", "two"→"2",
  "six"→"6"; "one" 은 유지 — 사용자 6건 + 원어민 TTS 3종 9/9 재현). `judgeCoverage` 가 Display 를 원문과
  대조해 그 단어를 누락으로 세었고, 원어민 TTS 3종이 "Dinner at seven sounds like a good idea." 에서
  77~78점이었다(누락 12.5 + floor 하락). 같은 응답의 `NBest[0].Lexical` 은 "dinner at seven …" 그대로.
  **수정**: `analyzeWavRest` 가 `recognizedLexical` 을 싣고, `coverageJudge.judgeCoverageOf`(Display·Lexical 중
  누락이 적은 쪽 — 어떤 문장에서도 종전보다 나빠지지 않음)로 `computeDeductionScore`·`judgeProduction`·
  `judgeMisread`·체이닝 통과 판정을 교체. 화면 전사는 Display 유지. 시드에 숫자 단어 문장은 46개(그중 "one"
  약 15개는 무영향). 실측 스크립트·PA 응답 픽스처: `~/.local/state/study-probe/pa-itn-probe-20260906/`.
- **발견 2 (서버 기록 227건, 8/31~9/6)**: 화면 점수 90 이상 0회(최고 89), Azure 자체 종합(PronScore)은 90
  이상 47회·평균 10.7점 높음. 억양 축이 227회 중 220회에서 최대 감점(평균 20.6, 단어 3.0, 누락 1.8, 끊김
  1.3). 90 은 억양 88.9 이상을 요구하는데 본인 최대 88.7, 원어민 TTS 억양은 5문장 7회 실측 90~91 이 천장 —
  100 기준 감점이 아무도 못 갖는 9점을 모두에게 부과했다. 8/31 보정의 목적 "격차 최대"(제약 원어민 ≥90)가
  원어민을 정확히 90 에 놓았고, 9/4 상한 20→30 은 하단만 내렸다(9/5~6 앱 평균 70.8·68.3, 억양은 그대로).
  **수정**(사용자 결정 "수정은 권장대로"): `DEDUCTION_RATES.intonation.anchor = 90` — 억양 감점 =
  max(0, 90−억양)×0.9, 상한 30 유지. `scoreForDisplay` 표식 `ded2`. 227회 재계산(잔차 유지): 평균 71→80,
  중앙 72→81, 90 이상 0→39회(17%, Azure 종합의 21% 와 비슷), 60 미만 22→12, 억양 나쁨(<70)·좋음(≥85)
  평균 차 22.8→24.9(9/4 요구 유지). 원어민 실측 픽스처는 엔진 통과 시 98/98/99.
- ⚠ **사용자 재확인 대기 (8/31 보정 제약과의 충돌)**: 유치1 실기록(억양 82.4)이 83→92, 끊어읽기 앵커(유창
  63·억양 86.5)가 76→85 — 8/31 제약 '유치 ≤87'·'끊어읽기 ≤82' 위반. 억양 82.4 는 본인 분포 상위 10%(p90
  83.1)라 "잘하면 90점대" 와 "이 발화는 유치" 가 같은 지점에서 충돌한다. 단가 1.5 면 87·82 로 복귀하나
  본인 중앙값 71(현행 72 와 거의 같음). 끊어읽기는 유창 축 상한 10 이 하한이라 억양 단가로는 못 내린다.
- ⚠ **짧은 문장**: 원어민 TTS 억양이 "Sure." 73.7, "Thank you." 86.2, "Thank you so much." 91 — 1~2단어는
  기준점 90 으로도 원어민이 85·96 에 그친다(현행 76·86~87). 2단어 이하 억양 축 제외 또는 길이별 기준점 표가
  필요하나 실측이 3문장뿐 — 결정·추가 실측 대기. 본인 기록엔 3단어 이하 행이 2건뿐.
- **지연 분석 (같은 날, 기록 기반)**: 늦게 뜬 점수는 전부 STT 첫 시도 실패→재시도 행(`sttAttempts` 2)과
  일치(9/3 이후 128건 중 6건, 시간대에 군집). 토큰·시간초과·화면 숨김 아님. 계측이 실패 사유(429/5xx/네트워크)
  를 안 남겨 확정 불가 — **meter 에 시도별 status 기록이 다음 과제**. 실 Azure 실측(Mac, 봇 토큰): abort 직후
  재요청 3회·2~3건 동시 7회 모두 200 → 9/4 "겹치면 429" 는 상시 상한이 아니다(9/4 원인 미확정). 5번 시도
  (6.5초)는 선채점 3초 대기 만료→끊고 재전송→그 요청도 재시도 였다.
- **주의**: DB `pron_score` 는 마이그레이션 0007 주석과 달리 `PronScore`(종합)다(`sync.js` 176). AccuracyScore
  는 LOCAL_ONLY. 문서 가중식(0.4·0.2·0.2·0.2)으로 역산 가능(원어민 픽스처 오차 1~3점).
- 검증: `pnpm test` 1521/1521 (신규 10건 + 기대값 갱신 8건), `pnpm build` 통과. 실기기 미확인.

## 남은 작업 (즉시 실행 가능)

1. **시드 수정분 DB 반영** — `seeds/en-core100-2026-08-26.json` 은 repo 만 갱신됐고
   Supabase 에는 2026-08-26 업로드본이 그대로다. `study-seed-supabase.yml` 워크플로를
   재실행해야 앱 화면에 반영된다 (id 기준 upsert). 절차는 [seeds/README.md](../seeds/README.md).
   ⚠ DB 변경이므로 사용자 확인 후 실행. `dry_run=true` 먼저. user_id 는 원 업로드와
   같은 계정이어야 한다 (Supabase Dashboard → Auth 에서 확인).
2. **배포 확인** — push 는 했으나 `pnpm build`·`pnpm preview`·실기기(iPhone Safari)
   확인은 하지 않았다. 채점 수정은 speech.js 클라이언트 코드라 배포돼야 실기기에 반영된다.
3. (선택) **실기기 스모크** — 짧은 드릴("What do you mean" 등)을 흘려 발음해 점수가
   80대에서 90대로 오르는지 체감 확인.

## 사용자 결정 대기 (새 세션에서 물어볼 것)

1. **§7 재분절 의무의 처분** — 유지 / 완화 / 폐기. 코퍼스 5,000여 건에 영향.
   - 권장 = **폐기**: 두 실측(STT 인식률·PA 채점)이 같은 방향. 연음은 `phonemes`·`tips`
     설명으로 가르치고 음차는 단어 경계를 지키는 쪽이 낫다. 시드의 해설 필드가 이미
     그렇게 하고 있다 (예: "'익스플레인 잇'이 붙어 '익스플레이닛'처럼 들린다").
   - 폐기 결정 시 후속: §7 표·문구 개정, validate-seed 경고 문구 갱신(또는 경고 제거),
     기존 코퍼스는 손대지 않음(재음차가 오히려 후퇴임이 실측됨).
2. **CONTRACTIONS 확장 여부** — `understood you` 류는 Azure 사전에 축약 철자가 없어
   여전히 깎인다. 새 항목은 반드시 실측 확인 후 추가 (없는 철자를 넣으면 그 요청이
   통째로 저점이 되어 해롭다). 확장 시 memory 의 실측 경로 사용.

## 관찰 항목

- 축약이 있는 문장은 채점 STT 호출이 2회가 됐다 → F0 무료 한도(월 STT 5시간) 소모 증가.
  429 가 잦아지면 축약 병행을 취약 문장에만 한정하는 축소를 검토.
- 선채점(최대 3발)과 축약 병행(×2)은 **곱으로** 작용한다 — 축약 문장 최악 8호출/녹음
  (통상은 선채점 1발 재사용으로 2호출). 2026-09-01 선채점 임계 0.5초 인하로 중간 쉼 발사가
  다소 늘 수 있다. 429 관찰은 이 곱셈을 전제로 볼 것.
