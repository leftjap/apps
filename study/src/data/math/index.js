import { MODULE_COMPOUND } from './m1-compound.js';
import { MODULE_VISUAL } from './m2-visual.js';

// 학습 순서: 복리(정량추론) → 시각·기하 strand. 진단 결과 = 기초는 건너뛰고 프런티어부터.
export const MATH_CONTENT = [...MODULE_COMPOUND, ...MODULE_VISUAL];

export default MATH_CONTENT;
