# Dependencies
node_modules/

# Build artifacts
dist/
.next/
.cache/
.vite/

# Environment / secrets
.env
.env.local
.env.*.local

# OS
.DS_Store

# Editor
.vscode/
.idea/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Board (Electron, git 미업로드)
board/

# Claude scratch / personal
.scratch/
.notification-preview.html

# Sessions / status (폐기되었으나 보호)
.status-archive/
STATUS.md
STATUS-common.md

# Test coverage
coverage/

# Local Claude project settings (이미 있는 것 보존)
.claude/.scope-approved
.claude/.study-docs-read

# Supabase CLI link cache (자동 갱신)
**/supabase/.temp/

# 사용자 본문 포함 — 로컬 only (admin 백업 JSON·검증 스크린샷)
today/handoff/leftjap-navi-backup-*.json
today/handoff/soyoun-navi-backup-*.json
today/handoff-screenshots/

# Study 학습 소스 원문 (유료 구독 리얼클래스 콘텐츠 — 공개 repo 전문 커밋 금지, 시드 발췌만 커밋)
study/seeds/sources/

# 세션 핸드오프 — 로컬 전용 (공개 repo: 개인정보·검증 스크린샷 커밋 금지. CLAUDE.md "세션 핸드오프" 참조)
**/handoff/

# 검증 스크린샷 — 로컬 전용 (공개 repo: 개인 일기·가계부 등 사생활 노출 방지)
**/verify/

# goal-loop 마커 — 로컬 전용·트랜지언트 (활성 goal 상태. goal-harness 스킬·~/.claude/hooks/goal-loop.mjs)
**/.goal.json

# Swift Package Manager 빌드 산출물 (readingtime ReadingTimeKit)
.build/
readingtime/ReadingTimeKit/Sources/RTViews/Fonts/
# Xcode 개인 상태
**/xcuserdata/
**/.swiftpm/
