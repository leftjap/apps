# SCREENS — 화면별 상세 스펙 (v8 최종, 시안 `t5` 섹션 기준)

기준 뷰포트 390×844 (iPhone, 노치·홈 인디케이터 포함). 정확한 수치·색·그림자는 시안 HTML의 인라인 스타일이 원본이며, 아래는 구조·행동·카피의 규범이다. 모션 이름(`v5Fade` 등)은 `MOTION.md` 참조.

---

## 01 로그인
- **레이아웃:** paper-bg. 좌측 정렬 세로 중앙 블록 + 하단 고정 버튼. 좌우 패딩 32.
- 로고: 68×68 r19 그린 그라데이션 박스, 펼친 책 SVG 46px(리본 없음), `v4Float` 부유.
- 워드마크 "리딩타임" 40px/900/-.045em ink → 밑줄 44×3 그린 바(`v5Sweep`로 왼→오 드로우) → 카피.
- **카피(고정):** "당신의 리딩타임을 기록해 보세요.\n독서 습관이 바뀌면 책 읽기가 더 즐거워집니다." 15px/500 muted.
- Google 로그인 버튼: h56 r16 surface + hairline, Google G 20px + "Google로 계속하기" 16px/700.
- 하단 약관 캡션 11.5px faint. 진입 시 제목→카피→버튼 순 `v5Fade` 스태거(.05/.15/.25s).

## 02 홈 (허브)
- **헤더:** 로고 미니(26 박스+19 글리프) + "리딩타임" 17px/900 | 우측: +버튼(그린 원 34, 책 추가=13 시트) · 아바타(이니셜 34, 탭 → 문서 말미 §설정 노트).
- **히어로 카드** (surface r22): 표지 94×137(API 이미지, `v4Float` 8s) + 우측 열: "읽는 중" 라이브 칩(그린틴트, 점 blink) / 제목 21px/900 / 저자 12.5 muted / 하단 누적 `4:12`(mono 19/700)+"누적 · 8회".
  - 모드 세그먼트(2분할, seg-bg r13): **엎기**(활성=ink 배경) | **탭**. 아이콘 포함.
  - CTA "읽기 시작" h54 r15 그린 그라데이션 + ▶, 둘레에 `v5RippleBtn` 링 3s. 탭→03(엎기) 또는 05(탭).
- **스탯 스트립** (3등분): 오늘 `32분` | 이번 주 `7:26` | 연속 `12일`(terracotta). mono 16/700.
- **진입 카드 2개** (50/50): "서재"(미니 표지 3권 부채꼴, 카운트, → 12) · "기록"(7일 미니 바, 오늘=그린, "이번 주 7:26", → 10).
- **최근 기록** 리스트(라벨+"전체 보기"): 행 = 방식 아이콘 타일 30(엎기=그린틴트/밀리=앰버틴트) + `26분`(mono 14/700) + "몰입 · 엎기" + 우측 "오늘 14:14". 2행 노출.
- 진입 스태거: 히어로→스트립→최근 `v5Fade` 0/.08/.16s.

## 03 엎기 · 시작 대기 (다크)
- 다크 그라데이션 배경. 상단: 책 칩(미니 표지 22×31+제목) | "취소" 필.
- 중앙: 폰 글리프 58×92(골드 보더)가 `v6Flip`으로 3D 텀블 + 바닥 타원 그림자 `v7Shadow` 동기(뒤집힐 때 그림자 커짐). perspective 520.
- "폰을 엎어 주세요" 24px/800 `#f2eedd`. 아래 `00:00:00` 38px mono `#3d4f42` `v6Breath`.
- 하단: 고스트 버튼 "탭 모드로 전환" (h50, 유리 스타일). **행동:** 기기 face-down 감지 → 기록 시작, 화면은 04로.

## 04 엎기 · 일시정지 (다크) — 엎기 모드의 기본 가시 화면
- 상단: 책 칩 | "일시정지됨" 앰버 필(⏸ 12px + 텍스트 `#e8be78`).
- 중앙: 흰 원형 엠블럼 96(래디얼 하이라이트) 안에 **▶ 재생 글리프**(`#26413a`) — **탭=재개**. 둘레 앰버 점선 링(dasharray 3 9)이 `v6Spin` 26s로 서행 회전. 엠블럼 아래 캡션 "탭하여 이어 읽기" 11px.
- 경과 시간 `00:26:14` 56px mono, `v8Dim`으로 숨쉬듯 디밍(정지 상태 표현 — 콜론 점멸 없음).
- 힌트 행: **미니 폰 글리프(11×17)가 `v6Flip`으로 실제 뒤집힘** + "다시 엎으면 이어서" 13.5px `#8fa393`.
- 하단: 유리 카드(이 세션 `26분` | 오늘 누적 `58분`) + 라이트 CTA "여기까지 읽기"(h54, `#f2eedd` 배경/그린 텍스트, ✓) → 06.
- **행동:** face-down 재감지 또는 엠블럼 탭 → 재개(기록 중 상태로: 링 정지→물결 재개, 콜론 점멸 재개).

## 05 탭 모드 · 기록 중 (다크)
- 상단: "탭 모드" 칩 | "종료" 필. 배경에 골드 동심원 물결 3겹 `v5Ripple`(4.2s, 1.4s 간격) — **기록 작동 중임을 상시 표현.**
- "기록 중" 골드 필(점 blink) + 라이브 타이머 62px mono: 시(hh)는 40% 투명, **콜론 2개가 1s `v5Tick` 점멸**. 초가 실시간 증가.
- 책 표기: "미하이 칙센트미하이, 《몰입》" 13px.
- **탭 존:** 하단 22px 마진, h168 r24 점선 보더(골드 40%) + `v6Pulse`. 내부: 탭 아이콘 원 52(둘레 `v7Tap` 파동 링) + "화면을 탭하면 일시정지" 15/700. 존 전체가 히트영역.
- 최하단 mono 캡션 "두 번 탭 = 종료" 10px. **행동:** 존 탭=일시정지/재개 토글(일시정지 시 물결 정지·콜론 점멸 정지), 더블탭 또는 종료 필=06. 탭/더블탭 구분은 ~250ms 디바운스로 처리.

## 06 세션 완료
- paper-bg. 체크 원 88(그린 그라데이션) `v5Pop` + 체크 패스 `v5Draw` 드로우 + 둘레 `v5Ripple`.
- "기록됐어요" 14/700 muted → `26:14` 52px mono/700 ink → 칩 2개: [⟲ 엎기 · 자동](그린틴트) [몰입](seg-bg). 순차 `v5Fade` .5/.65/.8s.
- **오늘의 리딩타임 카드:** 우측 상단 `32분 → 58분`(58분은 2.2s에 그린으로 페이드인). 바 h12: 기존 36% `#3a5c4b` + **신규 29% `#729a80`가 1.2s에 `v5Sweep`로 자라남** + 그 위 `+26분` ink 칩이 1.9s에 `v5Drop`으로 낙하. 축 라벨 0분/1시간.
- 레저 카드: 시작 `14:14` / 종료 `14:40` / 일시정지 `2회 · 3분` (mono 값, hairline 행).
- CTA "저장하기" h54 그린 → 홈. 아래 "이 기록 삭제" 12.5/600 `#b56a55`.

## 07 시간 직접 추가 (바텀시트)
- 딤 배경 위 시트(sheet r26 상단, `v8Up` 슬라이드-업). 그랩 핸들 40×4.
- 제목 "시간 직접 추가" 20/900 + 닫기(×) 버튼.
- 책 행(surface r14): 미니 표지 30×42 + 제목/저자 + ›(책 변경).
- 스테퍼: [−] 원 46 | **`35` 56px mono `v5Pop`(값 변경 시마다 팝)** + "분" | [+] 그린 원 46.
- 프리셋 칩: `+5` `+10` `+15` `+30` (mono, 선택=ink 배경).
- "일시" 행: "오늘 · 14:14" + › (날짜/시간 피커).
- CTA "35분 추가하기"(선택 값 반영) h54 그린 → Session(method:'manual') 저장.

## 08 책 상세
- 헤더: ‹ 뒤로 | ⋯ 메뉴(책 삭제 등).
- 상단 블록: 표지 104×152 + 우측: "읽는 중" 라이브 칩 / 제목 23/900 / "저자 · 출판사" 12.5 / 누적 `4:12` 28px mono + "누적 · 8회 · 18일째".
- CTA 행: "이어서 읽기"(그린, flex 1.6) + "완독"(아웃라인, flex 1) → 09 시트.
- **기록 리스트:** 라벨 "기록" + 우측 [🕐 직접 추가] 버튼(07 오픈). 행 = 방식 아이콘 타일 32(엎기=그린틴트 ⟲ / 탭=손 / 직접=시계, 회색틴트) + `26분` mono 15/700 + "엎기 · 14:14" 12px + 우측 날짜("오늘"/`5.20`). hairline 구분. method별 아이콘 구분이 필수.

## 09 완독 (바텀시트)
- 시트 `v8Up`. 표지 78×114 `v5Pop` + 우하단 그린 체크 배지 30 `v5Pop`(.4s) + 둘레 `v5Ripple`.
- "다 읽었어요" 24/900 + "몰입 · 미하이 칙센트미하이 · 18일 동안".
- "이 책, 어떠셨나요?" 12.5/700 그린 → **별 5개 34px, `v7Star`로 회전 팝 스태거(.08s 간격)**, 앰버 `#c9973b`. 선택 라벨 "아주 좋았어요".
- 스탯 타일 3: 총 시간 `4:12` / 세션 `8회` / 함께한 기간 `18일`.
- CTA "완독으로 저장" → status:'finished', rating 저장, 서재 완독 그리드로.

## 10 기록 · 주간
- 헤더: ‹ + "기록" | 세그먼트 [주|월].
- 기간 mono `5.15 – 5.21` → 헤드라인 "이번 주 7시간 26분"(숫자 mono) → 델타 칩 [↑ 52분](그린틴트) + mono "vs 지난주".
- **주간 차트 카드:** 단색 바 7개(sand `#d6cba9`, r7, `v5Stack` 스프링 스태거 .06s). 오늘(목)=그린 그라데이션+외곽 링, 값 라벨 mono 9px(목=그린/700), 일요일 라벨 terracotta. 값(분): 52·74·43·96·63·34·84 — **주합 446분 = 헤드라인 7:26 = 책별 합계(4:12+1:36+1:38)와 일치.**
  - **선택일 팝오버:** ink 카드(r13, min-w150) — `5.21 목 · 96분` + 책별 행(색 점+제목+분: 몰입 64분 / 돈의 심리학 32분) + 2px 커넥터가 해당 바 중심에 정확히 연결. `v7TipPop`(translateX 유지 팝). **바 탭으로 선택일 변경** — 책이 몇 권이든 팝오버 목록으로 스케일.
- **밀리의서재 카드(슬림):** 모니터 아이콘 타일(앰버틴트, 우상단 동기화 아이콘 `v6Spin` 5s) + "밀리의서재" + mono "오늘 07:00 동기화" | 우측 mono "PC 1:12 · 모바일 0:26".
- 카드 2열: **연속** `12` 22px mono terracotta + "일 연속" + 14점 스트립(최근일수록 진한 terracotta, 마지막 점 `v4Blink`+링) / **시간대** "주로 밤 9–11시" 13/800 + 24h 스펙트럼 바(21–23시 구간 그린 `v6Breath`) + 축 06/12/18/24.
- **이번 주 많이 읽은 책:** 행 = 미니 표지 30×43 + 제목(밀리 책은 mono "밀리" 태그) + 진행 바(각 책 색, `v5Sweep` 스태거) + mono 누적. 3행(몰입 4:12 / 돈의 심리학 1:36 / 도둑맞은 집중력 1:38).

## 11 기록 · 월간
- 헤더 동일, 세그먼트 [주|월]=월 활성.
- "2026년 5월" 26/900 + ‹ › 월 이동. 요약 행: `21:08` 총 시간 · `19` / 21일 읽음 · "밀리 포함". (표지 있는 날 = 1–6, 8, 10–21일 = 19일과 일치)
- **캘린더:** 요일 헤더(일=terracotta). 셀 = 날짜 mono 10 + **그날 가장 읽은 책의 표지 썸네일 28×40**(스파인 2px). 오늘=terracotta 2px 링+날짜 강조. 완독일=표지 아래 그린 점 5px. 밀리로만 읽은 날은 그 전자책 표지. 미래일은 faint 날짜만. 그리드 전체 `v5Fade` 인.
- 범례: "● 완독" 한 줄만.

## 12 서재
- 헤더: ‹ + "서재" + mono 카운트 | + 버튼(→13).
- **검색 필드** h44 r13 (`#efeadb`): 돋보기 + 플레이스홀더 "내 책 · 저자 검색".
- 필터 세그먼트 [전체|읽는 중|완독] + 우측 **정렬 칩 [⇅ 최근순]** — 탭 시 정렬 옵션 시트(최근순/이름순/별점순)를 열 것(시트 자체는 시안 외 스펙, 07 시트 문법을 따름).
- "읽는 중" 카드: 표지 54×79 + 제목/저자 + mono `4:12 · 8회 · 18일째` + 우측 ▶ 그린 원 40(바로 03/05 시작).
- "완독 13" 그리드 3열: 표지 86×124(그림자, `v5Fade` .05s 스태거) + 아래 별점(앰버 채움/빈 별). 페이지 진행률 UI 없음(모델에 없음).

## 13 책 추가 (바텀시트)
- 시트 `v8Up`(top 96 고정). 제목 "책 추가" + ×.
- 검색 인풋 h50 r14 그린 1.5px 보더 포커스 상태 + 커서 캐럿 blink. 아래 mono "검색 결과 · 32건".
- 결과 행: 표지 46×64 + 제목 14.5/700 + "저자 · 출판사" | 우측 상태 버튼: 미추가=+(seg-bg 원), 추가됨=✓(그린 원, `v5Pop`). 첫 행은 추가됨 상태 + 그린틴트 행 배경. **UI 어디에도 "알라딘" 문구 노출 금지.**

## 14 홈 · 빈 상태
- 02와 동일 헤더(+ 버튼 없음, 아바타만).
- 히어로 카드: 점선 표지 자리 88×128(+ 아이콘, `v4Float`) + "무슨 책부터\n시작해 볼까요?" 20/900 + CTA "첫 책 추가하기"(+ 아이콘, `v5RippleBtn`) → 13.
- 스탯 스트립: `0분` / `0:00` / `—` (전체 ghost 색, opacity .75).
- 하단: 점선 책 3권 실루엣 + 선반 라인(설명 문구 없음).

---

## 설정 (시안 외 스펙 — 별도 화면 없음)
홈 아바타 탭 → 심플 바텀시트(07 시트 문법): **이름 수정 / 밀리의서재 연동 상태(연결됨 표시·해제) / 로그아웃**. 별도 탭·전용 화면을 만들지 말 것 — "홈 허브 + 시트" 내비 원칙 유지.
